version: 0.2

 

phases:
  build:
    commands:
    - echo Entered the build phase..
    - mvn clean install
  post_build:
    commands:
    - echo Build completed
artifacts:
  files:
    - target/*.jar
  discard-paths: yes